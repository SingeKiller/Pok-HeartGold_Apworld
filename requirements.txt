# requirements.txt
#
# Runtime dependencies of the Pokemon HGSS APWorld itself. As of the
# ndspy -> apnds migration, this world has none: `apnds` (MIT, James
# Petersen) is vendored directly in this repository (see /apnds/,
# /apnds_version.txt) and packaged inside the built `.apworld` (see
# .apignore), so nothing needs to be separately installed into the
# environment/venv that runs Archipelago anymore -- unlike `ndspy`
# (GPLv3, could not be bundled this way; see docs/architecture.md,
# "## Spikes" - Spike 1, for the historical ndspy-based design this
# replaced).
#
# This file is intentionally left with no entries below this comment.
